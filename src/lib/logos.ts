// Placeholder until the real Avid Associates logo files are uploaded directly
// (as image attachments, not pasted base64) and dropped into /public.
export const LOGO_ICON_SRC: string | null = null;
export const LOGO_FULL_SRC: string | null = null;
