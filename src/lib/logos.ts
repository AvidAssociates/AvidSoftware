// The Avid Associates mark, served from /public/avid-mark.png.
export const LOGO_ICON_SRC: string | null = "/avid-mark.png";
// No separate wordmark lockup provided — Login renders the mark above the
// text wordmark instead of a single combined image.
export const LOGO_FULL_SRC: string | null = null;
